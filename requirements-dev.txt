# Development-only dependencies (not installed on Render).
#
# Install with:
#   pip install -r requirements-dev.txt
#
# Run the tests with:
#   pytest                          # curated data + /api/journey
#   node --test "tests/*.mjs"       # ticket-boundary calculator (no deps)
#   cd worker && npm test           # submission Worker (no deps)
#
# The two JS suites need Node 18+ and no package installs — they use the
# built-in test runner.

-r requirements.txt

pytest==8.2.2   # curated-data integrity checks in tests/
# The image scripts import PIL. It was never in either requirements file, so
# a clean checkout could not run scripts/build_icons.py or
# scripts/prepare_brand_mark.py — which is how "reproducible" quietly stops
# being true. Not in requirements.txt: the API never imports it.
pillow==10.4.0
