# requirements.txt — Python dependencies for Adur & Worthing Bus API
#
# Install with:
#   pip install -r requirements.txt
#
# Deployed automatically by Render from this file.

fastapi==0.111.0          # Web framework — lightweight, fast, great docs
uvicorn[standard]==0.30.1 # ASGI server for local development
httpx==0.27.0             # Async HTTP client for proxying BODS API calls
python-dotenv==1.0.1      # Load .env file for local development (optional)
tzdata==2024.1            # IANA timezone data for zoneinfo (Europe/London)

# Phase 2 — GTFS-RT real-time updates (already installed, used later)
protobuf==4.25.3
gtfs-realtime-bindings==1.0.0
